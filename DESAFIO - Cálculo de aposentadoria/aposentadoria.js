//Cálculo de aposentadoria

const nome = "Silvana";
const sexo = "F";
const idade = 48;
const contribuicao = 23;

const podeaposentar = idade + contribuicao;

const homemPodeAposentar
const mulherPodeAposentar

if( homemPodeAposentar = sexo == 'M'|| 'F' ){ //condição 1
    if(homemPodeAposentar = contribuicao >= 30    ){ //condição 2
        if(homemPodeAposentar = podeaposentar >=85 ){//condição 3
            console.log(`${nome} , você e do ${sexo} e tem a idade ${idade} e o tempo de contribuição de ${contribuicao}  `)
        } else{
            console.log(`${nome}, você e do sexo ${sexo} e não atingiu as outras condições ${contribuicao} e ${podeaposentar} `)
            }
        }

    }else{
        console.log(`${nome}, você e do sexo ${sexo} e tem ${contribuicao} de contribuição não atingiu a outra condições ${podeaposentar} `)
    }






